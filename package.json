{
  "name": "camp",
  "author": "nuoya",
  "version": "1.1.0",
  "description": "wia code camp",
  "scripts": {
    "build-rs": "cross-env NODE_ENV=production rspack build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cross-env NODE_ENV=development WIA_MODE=local gulp",
    "build": "cross-env NODE_ENV=production WIA_MODE=local gulp",
    "build:js": "cross-env NODE_ENV=production rspack build",
    "esbuild": "esbuild bin/entry.js --bundle --outfile=bin/out.js",
    "swc": "swc --config-file ./.swcrc src -w -d build",
    "pub": "cross-env NODE_ENV=development WIA_MODE=pub gulp --gulpfile wiamake.js build"
  },
  "keywords": [],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": ""
  },
  "homepage": "",
  "engines": {
    "node": ">=18.12.0"
  },
  "devDependencies": {
    "@freecodecamp/curriculum-helpers": "^3.7.0"
  },
  "dependencies": {}
}
